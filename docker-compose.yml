version: '3'

services:
  serverless:
    container_name: serverless
    build: .
    restart: always
    ports: 
      - "4050:4050"
    command: /bin/bash
    volumes:
      - ./:/usr/src/app
      - ./node_modules:/usr/src/app/node_modules
    env_file:
      - .env